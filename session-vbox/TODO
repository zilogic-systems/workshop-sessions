== TODO

 * Need to set bidirectional shared clipboard when creating disk image.

 * Need to add USB filters for USB-UART and USB-Ethernet when creating disk image.

 * Add proper configuration for network interfaces in /etc/network/interfaces.

== Known Issues

 * Vagrant adds the default shared folder mapping, but fails to mount it.

 * Packed Vagrantfile is not initialized for some reason.

 * Defining no. of cpus in "Vagrantfile" does not work.
